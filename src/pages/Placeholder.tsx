import { Construction, ShieldCheck } from "lucide-react";
export function Placeholder({title}:{title:string}) { return <div className="mx-auto max-w-4xl"><h1 className="text-3xl font-black">{title}</h1><div className="mt-8 rounded-3xl border border-base-300 bg-base-100 p-12 text-center shadow-sm"><div className="mx-auto grid size-16 place-items-center rounded-2xl bg-primary/10 text-primary">{title.includes("Safety")?<ShieldCheck/>:<Construction/>}</div><h2 className="mt-5 text-xl font-extrabold">Core foundation is ready</h2><p className="mx-auto mt-2 max-w-lg text-sm leading-6 text-base-content/55">This area is reserved for the next production module. Device enrollment, usage insights, permission health, rules, and the audit trail are already connected.</p></div></div> }
